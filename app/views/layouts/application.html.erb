<!DOCTYPE html>
<html>
  <head>
    <title>X-Wing</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= stylesheet_link_tag 'application', media: 'all'%>
    <%= csrf_meta_tags %>
  </head>

  <% if signed_in? %>

    <body>
      <header>
        <nav class="header-nav">
          <ul class="header-list">
            <a href= "<%="#/profile/#{current_user.id}"%> " id="user_form"></a>
            <li class="header-list-item"><%= button_to "Sign Out", session_url, :method => :delete %></li>
          </ul>
        </nav>
      </header>
      <%= yield %>
    </body>

  <% else %>

    <%= link_to "Sign Up", new_user_url, class: "btn btn-primary" %>
    <%= link_to "Log In", new_session_url, class: "btn btn-primary" %>
    <%= yield %>

  <% end %>
</html>
